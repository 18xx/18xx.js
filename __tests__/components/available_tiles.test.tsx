import * as React from 'react';
import * as renderer from 'react-test-renderer';

import AvailableTiles from '../../src/components/available_tiles';

describe('AvailableTiles', () => {
  it('is pending');
});
